import supabase from "../database/supabase.js";

class UploadRepository {
    async compareData(csvData) {
        // Lógica para comparar colunas do CSV com o banco de dados
    }
}

export default UploadRepository;